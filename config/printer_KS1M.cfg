# This file contains common configurations and pin mappings for the Anycubic
# Kobra S1 MAX (KS1M) printer.
#
# INITIAL / BETA VERSION - derived from printer_KS1.cfg with the hardware and
# geometry values extracted from the stock go-klipper printer.cfg of the
# KS1 MAX (device_type: "Anycubic Kobra S1 Max", print_size 350*350*350mm).
# Everything marked "VERIFY" below could not be confirmed against real
# hardware yet - check those before the first unattended print.
#
# Requires (via include below): printer_generic_macros.cfg for PAUSE/RESUME
# scaffolding, insert/runout prompt helpers, VEL_PUSH/POP, load/unload/purge
# helpers; ace_KS1M.cfg for ACE-specific macros and optional
# _ACE_STATE/_ACE_HANDLE_PRINT_END.
# See docs/Config_Reference.md for a description of parameters.
[include mainsail.cfg]
[include printer_generic_macros.cfg]

######### Must be placed above ace.cfg if you want to use them with ACE Pro driver #########

# KS1 MAX toolhead filament detection.
# Unlike the KS1 the MAX has NO static filament-presence switch in the toolhead
# (stock go-klipper leaves [filament_hub] switch_pin empty and uses a
# [filament_motion_sensor] with pin_a/pin_b instead). The two phase-shifted
# encoder channels are mapped onto filament_tracker here - same approach as
# the K3M config.
[filament_tracker filament_runout_nozzle]
# KS1 MAX motion sensor (quadrature-like pulses):
# - tracker_detect_pin and tracker_encoder_pin are the two phase-shifted channels
# - stock go-klipper: [filament_motion_sensor] pin_a: nozzle_mcu:PA5 / pin_b: nozzle_mcu:PA6
# VERIFY: pin polarity. If the tracker reports "no filament" with filament
# loaded, prefix both pins with '!' (the K3M needs the inverted variant).
tracker_detect_pin: nozzle_mcu:PA5
tracker_encoder_pin: nozzle_mcu:PA6
signal_type: gpio
# go-klipper mm_per_pulse: theoretical 5.978 (29.89mm wheel / 5 holes), measured 5.02
length_per_pulse: 5.02
absence_timeout: 2
pause_on_runout: False
runout_gcode:
    {% set ps = printer.print_stats.state|string %}
    {% if ps in ["printing", "paused"] %}
        TO_THROW_POSITION
        RESPOND TYPE=command MSG="action:prompt_begin Filament Runout"
        RESPOND TYPE=command MSG="action:prompt_text Filament runout detected. Print is paused. Insert filament into the toolhead, it will be pulled in automatically."
        RESPOND TYPE=command MSG="action:prompt_button Cancel Print|RUNOUT_CANCEL_PRINT"
        RESPOND TYPE=command MSG="action:prompt_show"
    {% else %}
        { action_respond_info("ANY[m]: Runout ignored (state=%s)" % ps) }
    {% endif %}
insert_gcode:
    RESPOND TYPE=command MSG=action:prompt_end
    {% set ps = printer.print_stats.state|string %}
    {% if ps == "paused" %}
        M83
        G92 E0
        G1 E150 F300
        G92 E0
        FLUSH_POOP
        RESPOND TYPE=command MSG="action:prompt_begin Filament Loaded"
        RESPOND TYPE=command MSG="action:prompt_text Filament detected. Choose next action:"
        RESPOND TYPE=command MSG="action:prompt_button Extrude 50mm|INSERT_EXTRUDE_50"
        RESPOND TYPE=command MSG="action:prompt_button Resume Print|INSERT_RESUME_PRINT"
        RESPOND TYPE=command MSG="action:prompt_button Cancel Print|INSERT_CANCEL_PRINT"
        RESPOND TYPE=command MSG="action:prompt_show"
    {% elif ps not in ["printing", "paused"] %}
        RESPOND TYPE=command MSG="action:prompt_begin Filament Detected"
        RESPOND TYPE=command MSG="action:prompt_text Filament detected while printer is idle. Load filament to nozzle?"
        RESPOND TYPE=command MSG="action:prompt_button Load Filament (@245°C)|INSERT_LOAD_FILAMENT"
        RESPOND TYPE=command MSG="action:prompt_button Dismiss|INSERT_DISMISS"
        RESPOND TYPE=command MSG="action:prompt_show"
    {% else %}
        { action_respond_info("ANY[m]: Insert ignored (state=%s)" % ps) }
    {% endif %}

event_delay: 4.0
pause_delay: 1.5

##### Use this block instead if you did the filament presence switch hardware
##### modification in the toolhead (a real switch is far more robust than the
##### encoder). Comment out the [filament_tracker filament_runout_nozzle]
##### block above when you enable it.

# [filament_switch_sensor filament_runout_nozzle]
# pause_on_runout: True
# runout_gcode:
#     {% set ps = printer.print_stats.state|string %}
#     {% if ps in ["printing", "paused"] %}
#         TO_THROW_POSITION
#         RESPOND TYPE=command MSG="action:prompt_begin Filament Runout"
#         RESPOND TYPE=command MSG="action:prompt_text Filament runout detected. Print is paused. Insert filament into the toolhead, it will be pulled in automatically."
#         RESPOND TYPE=command MSG="action:prompt_button Cancel Print|RUNOUT_CANCEL_PRINT"
#         RESPOND TYPE=command MSG="action:prompt_show"
#     {% else %}
#         { action_respond_info("ANY[m]: Runout ignored (state=%s)" % ps) }
#     {% endif %}
# insert_gcode:
#     RESPOND TYPE=command MSG=action:prompt_end
#     {% set ps = printer.print_stats.state|string %}
#     {% if ps == "paused" %}
#         M83
#         G92 E0
#         G1 E150 F300
#         G92 E0
#         FLUSH_POOP
#         RESPOND TYPE=command MSG="action:prompt_begin Filament Loaded"
#         RESPOND TYPE=command MSG="action:prompt_text Filament detected. Choose next action:"
#         RESPOND TYPE=command MSG="action:prompt_button Extrude 50mm|INSERT_EXTRUDE_50"
#         RESPOND TYPE=command MSG="action:prompt_button Resume Print|INSERT_RESUME_PRINT"
#         RESPOND TYPE=command MSG="action:prompt_button Cancel Print|INSERT_CANCEL_PRINT"
#         RESPOND TYPE=command MSG="action:prompt_show"
#     {% elif ps not in ["printing", "paused"] %}
#         RESPOND TYPE=command MSG="action:prompt_begin Filament Detected"
#         RESPOND TYPE=command MSG="action:prompt_text Filament detected while printer is idle. Load filament to nozzle?"
#         RESPOND TYPE=command MSG="action:prompt_button Load Filament (@245°C)|INSERT_LOAD_FILAMENT"
#         RESPOND TYPE=command MSG="action:prompt_button Dismiss|INSERT_DISMISS"
#         RESPOND TYPE=command MSG="action:prompt_show"
#     {% endif %}
# event_delay: 3.0
# pause_delay: 0.5
# switch_pin: <your switch pin>


# ACE Pro return detection module (RDM, on the back of the printer)
# KS1  : main mcu PB0/PB1
# KS1M : main mcu PA5 (detect) / PA4 (encoder)  [stock go-klipper [filament_tracker]]
[filament_switch_sensor filament_runout_rdm]
pause_on_runout: False  # Enable this only if ACE is used to feed filament
switch_pin: PA5

############# Alternative RDM sensor handling via filament_tracker, comment out above filament_switch_sensor if you want to use this #############
# [filament_tracker filament_runout_rdm]
# tracker_detect_pin: PA5
# tracker_encoder_pin: PA4
# signal_type: adc
# safe_unwind_len: 150
# length_per_pulse: 1.86532063806894
# adc_inverted: True
# detect_pin_is_switch: True
# absence_timeout: 2
# #debug_trace: True
# pause_on_runout: True
# runout_gcode:


#[include spoolman_logic.cfg]  # has to be before ace.cfg
[include ace_KS1M.cfg]

#[include timelapse.cfg]

[gcode_macro _CLIENT_VARIABLE]
variable_use_custom_pos   : True ; use custom park coordinates for x,y [True/False]
variable_custom_park_x    : 105.0  ; custom x position; value must be within your defined min and max of X
variable_custom_park_y    : 352.0  ; custom y position; value must be within your defined min and max of Y
#variable_custom_park_dz   : 2.0   ; custom dz value; the value in mm to lift the nozzle when move to park position
#variable_retract          : 1.0   ; the value to retract while PAUSE
#variable_cancel_retract   : 5.0   ; the value to retract while CANCEL_PRINT
#variable_speed_retract    : 35.0  ; retract speed in mm/s
#variable_unretract        : 1.0   ; the value to unretract while RESUME
#variable_speed_unretract  : 35.0  ; unretract speed in mm/s
#variable_speed_hop        : 15.0  ; z move speed in mm/s
#variable_speed_move       : 100.0 ; move speed in mm/s
variable_park_at_cancel   : True ; allow to move the toolhead to park while execute CANCEL_PRINT [True/False]
variable_park_at_cancel_x : 105.0  ; different park position during CANCEL_PRINT [None/Position as Float]; park_at_cancel must be True
variable_park_at_cancel_y : 352.0  ; different park position during CANCEL_PRINT [None/Position as Float]; park_at_cancel must be True
## !!! Caution [firmware_retraction] must be defined in the printer.cfg if you set use_fw_retract: True !!!
#variable_use_fw_retract   : False ; use fw_retraction instead of the manual version [True/False]
#variable_idle_timeout     : 0     ; time in sec until idle_timeout kicks in. Value 0 means that no value will be set or restored
#variable_runout_sensor    : ""    ; If a sensor is defined, it will be used to cancel the execution of RESUME in case no filament is detected.
##                                   Specify the config name of the runout sensor e.g "filament_switch_sensor runout". Hint use the same as in your printer.cfg
## !!! Custom macros, please use with care and review the section of the corresponding macro.
## These macros are for simple operations like setting a status LED. Please make sure your macro does not interfere with the basic macro functions.
## Only  single line commands are supported, please create a macro if you need more than one command.
#variable_user_pause_macro : ""    ; Everything inside the "" will be executed after the klipper base pause (PAUSE_BASE) function
#variable_user_resume_macro: ""    ; Everything inside the "" will be executed before the klipper base resume (RESUME_BASE) function
#variable_user_cancel_macro: ""    ; Everything inside the "" will be executed before the klipper base cancel (CANCEL_PRINT_BASE) function
gcode:

#Enable this only if you have a MCU build with MCU debug command support
#[temperature_sensor mcu_temp]
#sensor_type: temperature_mcu
#sensor_mcu: mcu
#min_temp: 0
#max_temp: 100

#[temperature_sensor mcu_nozzle_temp]
#sensor_type: temperature_mcu
#sensor_mcu: nozzle_mcu
#min_temp: 0
#max_temp: 100

#For vanilla-klipper on SoC
# NOTE: on the KS1 MAX the nozzle mcu is /dev/ttyS0 (KS1 uses /dev/ttyS5)
#[virtual_sdcard]
#path: /useremain/app/gk/gcodes

#[mcu]
#serial : /dev/ttyS3
#restart_method : command
#baud : 576000

#[mcu nozzle_mcu]
#serial : /dev/ttyS0
#restart_method : command
#baud : 576000

#######################
#For tunneled-klipper:#
#######################
[virtual_sdcard]
path: ~/printer_data/gcodes

[mcu]
serial : /dev/ttyGS0
restart_method : command
baud : 576000

[mcu nozzle_mcu]
serial : /dev/ttyGS1
restart_method : command
baud : 576000

[printer]
kinematics : corexy
max_velocity:600
max_accel:20000
square_corner_velocity:15.0
# KS1 MAX has a much heavier gantry/bed than the KS1 -> lower Z limits
max_z_velocity:10
max_z_accel:400
#jump_speed_min : 95
#jump_speed_max : 120
#device_type: Anycubic Kobra S1 Max
#print_size: 350*350*350mm



[stepper_x]
step_pin : PC14
dir_pin : PC15
enable_pin : !PC13
microsteps : 16
full_steps_per_rotation:200
rotation_distance :39
position_endstop : -3
position_min : -3
position_max : 358
homing_speed : 30
homing_retract_dist:0
second_homing_speed:80
# KS1 MAX homes X off PA6. Unlike the KS1 (separate switch on PA8) this is the
# same pin the driver DIAG line sits on, i.e. the MAX homes sensorless.
# Alternative (true StallGuard endstop): comment the line below, use
#   endstop_pin: tmc2209_stepper_x:virtual_endstop
# and uncomment diag_pin in [tmc2209 stepper_x].
endstop_pin: PA6
#step_pulse_duration: 0.000002   # 2us

[stepper_y]
step_pin : PA0
dir_pin : PB12
enable_pin : !PC13
microsteps : 16
full_steps_per_rotation:200
rotation_distance :39
position_endstop : -2
position_min : -2
position_max : 380
homing_speed : 30
homing_retract_dist:0
second_homing_speed:80
# See the note in [stepper_x]; virtual_endstop alternative is
#   endstop_pin: tmc2209_stepper_y:virtual_endstop
endstop_pin: PA15
#step_pulse_duration: 0.000002   # 2us

[cs1237]
# Strain-gauge based nozzle probe. KS1 MAX moved all three lines compared to
# the KS1 (PA7/PA6/PA5 -> PA7 pulldown / PA15 / PB3) and uses a much lower
# trigger threshold.
level_pin:~nozzle_mcu:PA7
dout_pin:nozzle_mcu:PA15
sclk_pin:nozzle_mcu:PB3
register:60
sensitivity: -800
head_block_sensitivity:-300000
scratch_sensitivity:-100000
self_check_sensitivity:-400
block_filament_sensitivity:-3000


[probe_ks1]
pin : PB2
z_offset:0
x_offset:0
y_offset:0
speed : 3.0
final_speed : 4.0
lift_speed : 3.0
samples: 3
sample_retract_dist: 3
samples_result: median
# stock go-klipper uses 0.02 with 2 samples; 0.03 is a bit more forgiving with
# 3 samples/median on the large MAX bed
samples_tolerance: 0.03
samples_tolerance_retries:20


[bed_mesh]
speed: 300
horizontal_move_z:3
mesh_min:10,10
mesh_max:340,340
probe_count:7,7
mesh_pps: 2,2
split_delta_z:0.0125
algorithm: bicubic
fade_start: 1
fade_end: 10
fade_target: 0

[stepper_z]
step_pin : PA12
dir_pin : PD15
enable_pin : !PC13
microsteps : 16
rotation_distance : 2.66666666666666666
full_steps_per_rotation:200
endstop_pin : probe:z_virtual_endstop
position_min : -4
position_max : 353
homing_speed: 6
homing_retract_dist:4
second_homing_speed:3
#step_pulse_duration: 0.000002   # 2us

[extruder]
max_extrude_only_distance : 1200.0
step_pin : nozzle_mcu:PA12
# NOTE: unlike the KS1 the MAX extruder dir pin is NOT inverted
dir_pin : nozzle_mcu:PA11
enable_pin : !nozzle_mcu:PC13
microsteps : 16
full_steps_per_rotation:200
rotation_distance :5.0708945897
nozzle_diameter : 0.400
filament_diameter : 1.750
max_extrude_cross_section:50
heater_pin : nozzle_mcu:PB8
sensor_type : NTC ZM3486 HNS112HC4570G0060Y3486
sensor_pin : nozzle_mcu:PA0
control : pid
pid_Kp : 17.393
pid_Ki : 0.698
pid_Kd : 108.268
min_temp : -180
min_extrude_temp : 170
max_temp : 360
pressure_advance: 0.02

[heater_bed]
heater_pin : PB9
sensor_type : NTC 100K beta 3950
sensor_pin : PB0
control : pid
pid_Kp : 33.536
pid_Ki : 0.441
pid_Kd : 634.454
min_temp : -200
max_temp : 140  #NTC ADC error threshold
# Large AC-style bed on the MAX -> slow PWM cycle as in stock go-klipper
pwm_cycle_time:0.3



[tmc2209 stepper_x]
# The MAX uses a split UART (separate RX/TX) unlike the KS1 single-wire UART
uart_pin : PA3
tx_pin : PA2
uart_address : 3
run_current : 1.2
hold_current:0.75
sense_resistor: 0.0750
stealthchop_threshold :0
interpolate : True
driver_SGTHRS: 80
#diag_pin: PA6   # only with endstop_pin: tmc2209_stepper_x:virtual_endstop
driver_MULTISTEP_FILT: False

[tmc2209 stepper_y]
uart_pin : PA3
tx_pin : PA2
uart_address : 1
run_current : 1.2
hold_current:0.75
sense_resistor: 0.075
stealthchop_threshold :0
interpolate : True
driver_SGTHRS: 80
#diag_pin: PA15  # only with endstop_pin: tmc2209_stepper_y:virtual_endstop
driver_MULTISTEP_FILT: False

[tmc2209 stepper_z]
uart_pin : PA3
tx_pin : PA2
uart_address : 2
run_current : 1.0
hold_current:0.8
sense_resistor: 0.0750
stealthchop_threshold : 0
interpolate : True
driver_MULTISTEP_FILT: False

[tmc2209 extruder]
uart_pin : nozzle_mcu:PA3
uart_address : 3
run_current : 0.6
hold_current:0.3
sense_resistor: 0.075
# KS1 MAX runs the extruder in spreadCycle (KS1 uses stealthChop / 99999)
stealthchop_threshold : 0
interpolate : True
driver_SEMIN:15
driver_SEUP:3
driver_SEMAX:15
driver_SEDN:3
driver_SEIMIN:1
driver_MULTISTEP_FILT: False

# [safe_z_home]
# home_xy_position: 175,175
# speed: 120
# z_hop: 4.0
# z_hop_speed: 8.0

[idle_timeout]
timeout: 3600
gcode:
    {% if printer.pause_resume.is_paused %}
        M118 Bypassed Timeout
        M117 Bypassed Timeout
    {% else %}
        M118 Timeout Reached
        M117 Timeout Reached
        TURN_OFF_HEATERS
        M84
    {% endif %}

[homing_override]
axes: xyz
gcode:
  {% set REQX = 'X' in params or 'x' in params %}
  {% set REQY = 'Y' in params or 'y' in params %}
  {% set REQZ = 'Z' in params or 'z' in params %}
  {% if not REQX and not REQY and not REQZ %}
    {% set REQX = True %}{% set REQY = True %}{% set REQZ = True %}
  {% endif %}

  {% set CENTER_X = 175 %}
  {% set CENTER_Y = 175 %}
  {% set XY_FEED  = 120*60 %}   # 120 mm/s -> 7200 mm/min
  {% set Z_HOP    = 3 %}
  {% set Z_HOP_F  = 8*60 %}     # 8 mm/s  -> 480 mm/min

  # Save state so feedrate doesn't leak out after homing
  SAVE_GCODE_STATE NAME=homing_state

  {% if 'z' not in printer.toolhead.homed_axes %}
      # Lift requires a homed Z; fake it to allow the safety hop before real homing
      SET_KINEMATIC_POSITION Z=0
      G91
      G0 Z{Z_HOP} F{Z_HOP_F}
      G90
  {% endif %}

  {% set need_y = REQY or (REQX and 'y' not in printer.toolhead.homed_axes) or (REQZ and 'y' not in printer.toolhead.homed_axes) %}
  {% set need_x = REQX or (REQZ and 'x' not in printer.toolhead.homed_axes) %}

  {% if need_y %} G28 Y {% endif %}
  {% if need_x %} G28 X {% endif %}

  # ---- if Z is requested, ensure XY are homed (for G28 Z alone cases)
  {% if REQZ %}
    {% if 'y' not in printer.toolhead.homed_axes %} G28 Y {% endif %}
    {% if 'x' not in printer.toolhead.homed_axes %} G28 X {% endif %}

    # move to safe center and home Z (probe)
    G0 X{CENTER_X} Y{CENTER_Y} F{XY_FEED}
    G28 Z
    G91
    G0 Z{Z_HOP} F{Z_HOP_F}
    G90
  {% endif %}

  # Restore feedrate so it doesn't leak F480 to subsequent commands
  RESTORE_GCODE_STATE NAME=homing_state MOVE=0


[verify_heater extruder]
max_error: 300
check_gain_time:30
hysteresis: 5
heating_gain:0.5

[verify_heater heater_bed]
max_error: 120
check_gain_time:300
hysteresis: 5
heating_gain:0.5


[controller_fan controller_fan]
pin : PB3
fan_speed : 1
idle_speed: 0
idle_timeout: 30

[heater_fan extruder_fan]
pin : nozzle_mcu:PB6

[fan]
pin : nozzle_mcu:PB9
cycle_time:0.00005
# Only enable if your MCU firmware is built with pulse_counter support,
# otherwise Klipper aborts at startup with an unknown command error.
#tachometer_pin :nozzle_mcu:PB5
hardware_pwm: True

[fan_generic box_fan]
# auxiliary / side blower
pin : PA11
cycle_time: 0.00003333333
hardware_pwm: True
# pwm_table is a go-klipper extension and is NOT supported by vanilla Klipper.
# Stock table for reference:
# 0.0,0  0.1,0.10  0.2,0.11  0.3,0.124  0.4,0.13  0.5,0.14
# 0.6,0.155  0.7,0.18  0.8,0.21  0.9,0.28  1.0,0.45

[fan_generic air_filter_fan]
pin : PB5
cycle_time: 0.00003333333
hardware_pwm: True
# Stock go-klipper pwm_table (unsupported by vanilla Klipper), for reference:
# 0.0,0  0.1,0.023  0.2,0.026  0.3,0.03  0.4,0.038  0.5,0.047
# 0.6,0.06  0.7,0.08  0.8,0.14  0.9,0.224  1.0,0.40

[fan_generic chamber_fan]
# chamber circulation fan - MAX only
pin: PB6
cycle_time: 0.00003333333
hardware_pwm: True

[fan_generic exhaust_fan]
# external exhaust fan - MAX only.
# Stock go-klipper additionally has an [exhaust_fan] control section
# (fan_speed_initial: 60, tempthreshold_fan_on: 35) that turns this on
# automatically above 35C chamber temp. That module does not exist in vanilla
# Klipper - drive it manually via SET_FAN_SPEED FAN=exhaust_fan SPEED=...
pin: PB4
cycle_time: 0.00003333333
hardware_pwm: True
# Stock go-klipper pwm_table (unsupported by vanilla Klipper), for reference:
# 0.0,0  0.1,0.025  0.2,0.026  0.3,0.03  0.4,0.038  0.5,0.047
# 0.6,0.054  0.7,0.08  0.8,0.12  0.9,0.245  1.0,1.0

##########################################################################
# Active chamber heating (MAX only) - DISABLED, needs verification
##########################################################################
# Stock go-klipper uses a custom [chamber_temp] module that reads the chamber
# thermistor through the SoC IIO ADC
# (/sys/bus/iio/devices/iio:device0/in_voltage1_raw) - vanilla Klipper cannot
# do that. The stock config also carries a commented "sensor_pin: PA7", which
# suggests the NTC is additionally wired to main-mcu PA7 (that is why stock has
# a [duplicate_pin_override] for PA7, shared with the motor hw-version detect).
#
# VERIFY with QUERY_ADC before enabling: if PA7 really reads the chamber NTC,
# this gives you M141/M191 chamber control.
#
#[heater_generic chamber_heater]
#heater_pin: PB8
#max_power: 1.0
#sensor_type: NTC 100K beta 3950
#sensor_pin: PA7
#min_temp: -30
#max_temp: 65
#control: pid
#pid_Kp: 38.228
#pid_Ki: 1.499
#pid_Kd: 0.0
#
#[verify_heater chamber_heater]
#max_error: 999999
#check_gain_time: 3600
#hysteresis: 50
#heating_gain: 0.1

#[led_pin]
#gpio:117

[lis2dw12]
spi_speed: 1000000
cs_pin: nozzle_mcu:PB12
spi_software_sclk_pin:nozzle_mcu:PB13
spi_software_mosi_pin:nozzle_mcu:PB15
spi_software_miso_pin:nozzle_mcu:PB14
rate: 400
axes_map:x,z,y

[resonance_tester]
accel_chip: lis2dw12
probe_points:177, 177, 20
min_freq: 1
max_freq: 140
accel_per_hz: 150
hz_per_sec:  2 #max 2 allowed, orig AC was 4
#test_freq_start: 70
#test_freq_end: 80

[gcode_arcs]
resolution:1.0

[input_shaper]
shaper_freq_x: 60.0
shaper_freq_y: 60.0
shaper_type_x: mzv
shaper_type_y: mzv

[exclude_object]

[force_move]
enable_force_move: True

[stepper_enable]

# Actually not a filament sensor, but a switch for the filament cutter
# True=Cutter engaged
# NOTE: on the MAX the cutter switch is inverted compared to the KS1
# (stock go-klipper: cutter_pin: !nozzle_mcu:PB1)
# [filament_switch_sensor filament_cutter]
# pause_on_runout: False
# #runout_gcode:
# #insert_gcode:
# event_delay: 3.0
# pause_delay: 0.5
# #debounce_delay:
# switch_pin: !nozzle_mcu:PB1

[gcode_macro M106]
rename_existing: M106.1
description: "M106 with named fan mapping and ignore list"
gcode:
    {% set p = params.P|int if params.P is defined else 0 %}
    {% set s = params.S|int if params.S is defined else 255 %}
    {% if s < 0 %}
        {% set s = 0 %}
    {% elif s > 255 %}
        {% set s = 255 %}
    {% endif %}

    ; hardcoded ignore list
    {% set ignored_ps = [3] %}

    {% if p in ignored_ps %}
        ; M106: P{p} ignored
    {% else %}
        {% if p == 2 %}
            SET_FAN_SPEED FAN=box_fan SPEED={ s / 255.0 }
        {% elif p == 3 %}
            SET_FAN_SPEED FAN=air_filter_fan SPEED={ s / 255.0 }
        {% elif p == 1 %}
            ; M106: P1 not mapped
        {% else %}
            ; M106: route to default part-cooling fan
            M106.1 S{s}
        {% endif %}
    {% endif %}

[gcode_macro M107]
rename_existing: M107.1
description: "M107 off with named fan mapping and ignore list"
gcode:
    {% set p = params.P|int if params.P is defined else 0 %}

    ; hardcoded ignore list (must match M106)
    {% set ignored_ps = [3] %}

    {% if p in ignored_ps %}
        ; M107: P{p} ignored
    {% else %}
        {% if p == 2 %}
            SET_FAN_SPEED FAN=box_fan SPEED=0
        {% elif p == 3 %}
            SET_FAN_SPEED FAN=air_filter_fan SPEED=0
        {% elif p == 1 %}
            ; M107: P1 not mapped (heater fan is thermostatic)
        {% else %}
            ; M107: turn off default part-cooling fan
            M107.1
        {% endif %}
    {% endif %}

[gcode_macro CANCEL_PRINT]
description: CANCEL_PRINT
rename_existing: CANCEL_PRINT_BASE
gcode:
    #In case resume was called from CANCEL PROMPT, close the prompt
    RESPOND TYPE=command MSG=action:prompt_end

    # Clear Power Loss Recovery state to prevent false prompts on restart, if PLR is installed
    {% if "gcode_macro clear_last_file" in printer %}
        clear_last_file
    {% endif %}

    {action_respond_info("ACE[M]: CANCEL_PRINT, lifting Z")}
    G91
    G1 Z3 F600
    G90

    {% if printer["output_pin ACE_Pro"] is defined and printer["output_pin ACE_Pro"].value %}
        # A cancelled print resolves any pending tool-change-failed recovery
        # dialog (KlipperScreen/web dashboard) - clear it so a future print
        # never starts with a stale failure showing.
        ACE_CLEAR_TOOLCHANGE_FAILURE
        # Set CUT_TIP=0 if unload of current filament is not wanted if ACE is used
        _ACE_HANDLE_PRINT_END CUT_TIP=1
    {% else %}
        {action_respond_info("ACE[M]: ACE Pro disabled. Normal cut sequence")}
        # Comment the two commands here out if unload of current filament is not wanted if external spool is used
        CUT_TIP
        FORCE_MOVE STEPPER="extruder" DISTANCE=-90 VELOCITY=10
    {% endif %}
    TO_THROW_POSITION

    M104 S0
    M140 S0 ; turn off heatbed
    M106 S0 ; fan off
    SET_FAN_SPEED FAN=box_fan SPEED=0
    SET_FAN_SPEED FAN=air_filter_fan SPEED=0
    M84 # Disable steppers
    CANCEL_PRINT_BASE

[gcode_macro M204]
rename_existing: M204.1
description: M204 that supports single P or T (Anycubic Slicer compatibility)
gcode:
  {% set s = params.S|default(None) %}
  {% set p = params.P|default(None) %}
  {% set t = params.T|default(None) %}

  {% if s is not none %}
    # Standard: use S directly
    SET_VELOCITY_LIMIT ACCEL={s|float}

  {% else %}
    {% if p is not none and t is not none %}
      # Standard Klipper semantics: accel = min(P, T)
      {% if p|float < t|float %}
        {% set accel = p|float %}
      {% else %}
        {% set accel = t|float %}
      {% endif %}
      SET_VELOCITY_LIMIT ACCEL={accel}

    {% elif p is not none %}
      # Non-standard: only P given -> accel = P (match Anycubic / Go behaviour)
      SET_VELOCITY_LIMIT ACCEL={p|float}

    {% elif t is not none %}
      # Non-standard: only T given -> accel = T
      SET_VELOCITY_LIMIT ACCEL={t|float}

    {% else %}
      RESPOND PREFIX="M204" MSG="Ignored: no S/P/T specified"
    {% endif %}
  {% endif %}


######################################################################
# Wipe / purge-chute macros
#
# All coordinates below come from the stock go-klipper KS1 MAX macros:
#   throw / sweep position   X87  Y375
#   pre-sweep (safe staging) X105 Y352 .. Y375
#   wipe pad                 X137.5 .. X157.5 @ Y365..Y375
#   cutter                   X356.5 Y23 (approach X352)
######################################################################

[gcode_macro WIPE_NOZZLE1]
gcode:
    G90
    G1 F6000
    G1 Y375
    G1 X157.5 Y365
    G1 X137.5
    G1 X157.5 Y375
    G1 X137.5
    G1 X157.5
    G1 X137.5 Y365
    G1 X157.5
    G1 X137.5 Y375

[gcode_macro WIPE_ENTER]
gcode:
    VEL_PUSH
    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300
    G90
    G1 F6000
    G1 X111
    G1 Y348
    G1 X137.5
    G1 Y370
    VEL_POP

[gcode_macro WIPE_NOZZLE]
gcode:
    VEL_PUSH
    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300
    WIPE_NOZZLE1
    WIPE_NOZZLE1
    WIPE_NOZZLE1
    VEL_POP

[gcode_macro WIPE_STOP]
gcode:
    VEL_PUSH
    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300
    G90
    G1 F6000
    G1 Y370
    G1 X125.5
    VEL_POP

[gcode_macro WIPE_EXIT]
# No stock equivalent on the MAX - leaves the wipe pad towards the safe
# staging row so the following moves are collision free.
gcode:
    VEL_PUSH
    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300
    G90
    G1 F6000
    G1 Y370
    G1 X125.5
    G1 Y352
    VEL_POP

[gcode_macro NOZZLE_WIPE_SEQUENCE]
gcode:
        WIPE_ENTER
        WIPE_NOZZLE
        WIPE_STOP
        WIPE_EXIT

[gcode_macro RESUME_NOZZLE_WIPE_SEQUENCE]
gcode:
    NOZZLE_WIPE_SEQUENCE

[gcode_macro TO_THROW_POSITION]
gcode:
    VEL_PUSH
    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300
    G90
    G1 X105 F12000
    G1 Y352 F12000
    G1 Y370 F6000
    G1 Y375 F3000
    G1 X87 F1000
    VEL_POP

[gcode_macro TO_BLADE]
# VERIFY on hardware: scaled from the KS1 values (11mm in front of the throw row)
gcode:
    VEL_PUSH
    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300
    G90
    G1 F12000
    G1 X87
    G1 Y364
    VEL_POP

[gcode_macro TO_FRONT_OF_THROW_BLADE]
# Same staging point the stock EXIT_THROW_POSITION macro uses
gcode:
    VEL_PUSH
    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300
    G90
    G1 F15000
    G1 X105
    G1 Y352
    VEL_POP

[gcode_macro TO_FRONT_OF_THROW_BLADE_2]
gcode:
    VEL_PUSH
    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300
    G90
    G1 F15000
    G1 X105
    G1 Y300
    VEL_POP

[gcode_macro TO_FRONT_OF_THROW_BLADE_SLOW]
# VERIFY on hardware: scaled from the KS1 values (21mm in front of the throw row)
gcode:
    VEL_PUSH
    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300
    G90
    G1 F3000
    G1 X87
    G1 Y354
    VEL_POP

[gcode_macro MOVE_HEAT_POS]
# Park the nozzle over the silicone socket next to the wipe pad while cooling
# down (same X the stock WIPE_STOP ends on).
gcode:
    VEL_PUSH
    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300
    G90
    G1 F12000
    G1 Y352
    G1 X125.5
    G1 Y370
    VEL_POP

[gcode_macro UNDERLINE]
gcode:
    M83
    G90
    G92 E0
    G0 X5 Y4 F7200
    G1 Z0.30 F1200
    G1 X250 E10 F1200
    G1 Y5   E0.6 F1200
    G1 X5   E10 F1200
    G92 E0
    M82

[gcode_macro ZHOP_MIN]
description: Min z-hop that also works when Z is unhomed
gcode:
  {% set zh = params.Z|default(1.0)|float %}

  {% if "z" in printer.toolhead.homed_axes %}
    # Normal relative z-hop if Z is homed
    G91
    G1 Z{zh} F300
    G90
  {% else %}
    RESPOND PREFIX="warning" MSG="Z not homed, using FORCE_MOVE for z-hop"
    # Force Z move even when unhomed (positive Z should be away from bed)
    FORCE_MOVE STEPPER=stepper_z DISTANCE={zh} VELOCITY=5 ACCEL=50
  {% endif %}
  M400

######################################################################
# Cutter macro
#
# KS1 MAX cutter geometry (from stock [filament_hub] / MOVE_TO_IMPACT_POSITION):
#   impact_position_x            356.5   (approached via X352 - close to X max 358)
#   impact_position_y            23
#   cutter_position              13.0    (Y where the cutter switch triggers)
#   cutter_position_after_trigger 10.0   -> full stroke ends around Y3
#   skewing_position_x1/x2       352 / 347 @ skewing_speed 100  (post-cut skew)
######################################################################

[gcode_macro CUT_TIP]
description: Cut filament tip at the front-right cutter (Y jab, soft-limit safe)
# --- Tunables ---
variable_x_approach: 352.0      # safe X before creeping onto the cutter
variable_x_impact: 356.5        # cutter X
variable_y_impact: 23.0         # cutter Y (near front)
variable_cruise: 200.0          # mm/s (return)
variable_impact: 10.0           # mm/s (push)
variable_y_stroke: 20.0         # mm to push toward front (23 -> 3)
variable_repeats: 1
variable_dwell_ms: 80
variable_retract: 1.0
variable_skew_x1: 352.0         # post-cut skew, stage 1
variable_skew_x2: 347.0         # post-cut skew, stage 2
variable_skew_speed: 100.0      # mm/s
variable_park_x: 105.0
variable_park_y: 352.0
variable_speed: 12000
#set to -1 if front is Y-min; set to +1 if front is Y-max
variable_y_dir: -1

gcode:
    RESPOND TYPE=echo MSG="ACE[M]: CUT_TIP start"
    {% if "xy" not in printer.toolhead.homed_axes %}
        G28 Y X
    {% endif %}

    SAVE_GCODE_STATE NAME=ks1m_cut_state

    # Geometry & safety
    {% set safety = 0.5 %}
    {% set y_min = printer.toolhead.axis_minimum.y|float %}
    {% set y_max = printer.toolhead.axis_maximum.y|float %}
    {% set y_dir = printer["gcode_macro CUT_TIP"].y_dir|int %}
    {% if y_dir not in [-1,1] %}{% set y_dir = -1 %}{% endif %}

    # Choose a safe start Y so the jab (y_dir * y_stroke) stays in-bounds
    {% if y_dir == -1 %}
        # Front at low Y -> we will move toward smaller Y during the jab
        {% set min_start = y_min + y_stroke + safety %}
        {% set start_y = (y_impact if y_impact > min_start else min_start) %}
    {% else %}
        # Front at high Y -> we will move toward larger Y during the jab
        {% set max_start = y_max - (y_stroke + safety) %}
        {% set clamped = (y_impact if y_impact < max_start else max_start) %}
        {% set start_y = (clamped if clamped > y_min else y_min + safety) %}
    {% endif %}

    # Move to cutter: stage away from the right wall first, then creep in X
    G90
    G1 X{ x_approach - 20 } Y{ 40 } F{ cruise*60 }
    G1 X{ x_approach } Y{ start_y } F{ cruise*60 }
    G1 X{ x_impact } F{ 100*60 }
    M400

    G91
    {% for i in range(repeats|int) %}
        # Jab toward the cutter (front)
        G1 Y{ y_dir * y_stroke } F{ impact*60 }
        M400

        # Dwell at impact
        G4 P{ dwell_ms }
    {% endfor %}
    G90
    M400

    FORCE_MOVE STEPPER="extruder" DISTANCE=-2 VELOCITY=10
    FORCE_MOVE STEPPER="extruder" DISTANCE=2 VELOCITY=10
    FORCE_MOVE STEPPER="extruder" DISTANCE=-2 VELOCITY=10

    # Post-cut skew (stock: skewing_position_x1 / x2 at skewing_speed)
    G1 X{ skew_x1 } F{ skew_speed*60 }
    G1 X{ skew_x2 } F{ skew_speed*60 }

    # Staging moves
    G1 Y{park_y} F{speed}
    G1 X{park_x} F{speed}
    TO_THROW_POSITION
    FORCE_MOVE STEPPER="extruder" DISTANCE=2 VELOCITY=10

    # Restore modes/feeds (not position)
    RESTORE_GCODE_STATE NAME=ks1m_cut_state MOVE=0 MOVE_SPEED=200


[gcode_macro PRINT_END]
description: PRINT_END
gcode:
    {% set _cut_raw = (params.CUT_TIP | default(params.cut_tip) | default("1")) | string %}
    {% set _cut_raw = (_cut_raw | trim | replace('=', '', 1) | lower) %}
    {% set do_cut = (1 if _cut_raw in ['1','true','yes','on'] else 0) %}

    # Clear Power Loss Recovery state to prevent false prompts on next restart
    {% if "gcode_macro clear_last_file" in printer %}
        clear_last_file
    {% endif %}

    {action_respond_info("PRINT_END, lifting z")}
    G91
    G1 Z3 F600
    G90

    {% if printer["output_pin ACE_Pro"] is defined and printer["output_pin ACE_Pro"].value %}
        _ACE_HANDLE_PRINT_END CUT_TIP={do_cut}
    {% endif %}

    SET_VELOCITY_LIMIT ACCEL=20000 ACCEL_TO_DECEL=5000 VELOCITY=300

    G1 E-1 F600  # Tiny retract to reduce oozing

    # Note: CUT_TIP and filament retraction is handled by _ACE_HANDLE_PRINT_END
    # Only do manual cut/retract if ACE is disabled
    {% if not (printer["output_pin ACE_Pro"] is defined and printer["output_pin ACE_Pro"].value) %}
        {% if do_cut %}
            {action_respond_info("Cutting filament")}
            CUT_TIP
            TO_THROW_POSITION
            {action_respond_info("Retracting filament")}
            G1 E-100 F600  # Retract filament fully out of extruder
        {% else %}
            {action_respond_info("Skipping CUT_TIP")}
            TO_THROW_POSITION
        {% endif %}
    {% else %}
        # ACE handled the cut/retract, just move to throw position
        TO_THROW_POSITION
    {% endif %}


# =======================
# G9111 — Startup Macro for Anycubic slicer compatibility
# =======================
[gcode_macro G9111]
description: "Startup: Smart Skip (Same Tool) -> Sensor Check (Load) -> Heavy Purge (Clean), TOOL required only when ACE Pro is active"
variable_probing_temp: 140 # max nozzle temp while probing (e.g. Cartographer limit ~150C, keep margin)

gcode:
  # 1. Read parameters
  {% set bed_raw = (params.BEDTEMP | default(params.S) | default("60")) | string %}
  {% set noz_raw = (params.EXTRUDERTEMP | default(params.T) | default("200")) | string %}
  {% set BEDTEMP = (bed_raw | replace('=', '', 1)) | float %}
  {% set EXTRUDERTEMP = (noz_raw | replace('=', '', 1)) | float %}

  # --- Wipe temperature ---
  {% set calculated_wipe = [ (EXTRUDERTEMP * 0.75), 150 ] | max %}
  {% set WIPETEMP = params.WIPETEMP | default(calculated_wipe) | float %}

  # --- Probing temperature (max nozzle temp for any probing move) ---
  {% set probe_raw = (params.PROBING_TEMP | default(printer["gcode_macro G9111"].probing_temp) | string) %}
  {% set PROBING_TEMP = (probe_raw | replace('=', '', 1)) | float %}

  # --- ACE Pro pin state + tool parameter handling ---
  {% set ace_pin_defined = printer["output_pin ACE_Pro"] is defined %}
  {% set ace_pin_enabled = (ace_pin_defined and (printer["output_pin ACE_Pro"].value | float > 0.5)) %}
  {% set has_tool_param = ('TOOL' in params) or ('tool' in params) %}
  {% if ace_pin_enabled and not has_tool_param %}
      { action_raise_error("G9111: Missing required TOOL parameter. Use TOOL=[initial_tool]") }
  {% endif %}
  {% if has_tool_param %}
      {% set TOOL = ((params.TOOL | default(params.tool) | string | replace('=', '', 1)) | int) %}
  {% else %}
      {% set current_extruder = printer.toolhead.extruder %}
      {% set tool_num = current_extruder | replace('extruder','') %}
      {% set tool_num = tool_num if tool_num|length > 0 else '0' %}
      {% set TOOL = tool_num | int %}
      { action_respond_info("G9111: TOOL not passed and ACE Pro is disabled - using active tool T%d" % TOOL) }
  {% endif %}
  {% if TOOL < 0 %}
      { action_raise_error("G9111: Invalid TOOL=%d. TOOL must be >= 0." % TOOL) }
  {% endif %}

  # --- SMART LOGIC  ---
  {% set local_skip_purge = False %}

  # 1. ACE status
  {% if printer.save_variables is defined and printer.save_variables.variables is defined %}
      {% set ace_index = printer.save_variables.variables.ace_current_index|default(-1)|int %}
      {% set filament_pos = printer.save_variables.variables.ace_filament_pos|default('none') %}
  {% else %}
      {% set ace_index = -1 %}{% set filament_pos = 'none' %}
  {% endif %}

  # 2. Retrieve sensor status (tracker on the MAX, switch after the hw mod)
  {% set is_physically_loaded = False %}
  {% if "filament_tracker filament_runout_nozzle" in printer.configfile.settings %}
       {% set is_physically_loaded = printer["filament_tracker filament_runout_nozzle"].filament_detected %}
  {% elif "filament_switch_sensor filament_runout_nozzle" in printer.configfile.settings %}
       {% set is_physically_loaded = printer["filament_switch_sensor filament_runout_nozzle"].filament_detected %}
  {% endif %}

  # 3. DECISION: Same tool AND printhead sensor
  {% if TOOL == ace_index and filament_pos == 'nozzle' and is_physically_loaded %}
      {% set local_skip_purge = True %}
      { action_respond_info("G9111: Smart Logic -> Tool %d already loaded & checked. EVERYTHING SKIPPED." % TOOL) }
  {% else %}
      # Debug Info im Log
      { action_respond_info("G9111: Purge Active. Reason: Tool match=%s (Req:%d/Act:%d), Pos=%s, Sensor=%s" %
          (TOOL == ace_index, TOOL, ace_index, filament_pos, is_physically_loaded)) }
  {% endif %}

  # --- Set startup_toolchange flag ---
  {% if printer["gcode_macro _ACE_STATE"] is defined %}
    SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=startup_toolchange VALUE=1
  {% endif %}

  # --- Heating & Homing ---
  M140 S{BEDTEMP}  ; heat bed
  M104 S{WIPETEMP} ; preheat for the pre-homing wipe (XY homing does not probe)

  G28 X Y ; XY only - Z homing waits until the nozzle is wiped clean and cooled

  # --- PRE-HOMING WIPE: Z homing probes with the nozzle, residue on the tip causes probe errors ---
  RESPOND TYPE=echo MSG="Hot wipe before Z homing ({WIPETEMP}C)..."
  MOVE_HEAT_POS ; park on silicone socket while reaching wipe temp
  {% if printer.extruder.temperature > (WIPETEMP + 2) %}
      M106 S255 ; fan-assisted cooldown (e.g. nozzle still hot from a canceled print)
  {% endif %}
  M109 S{WIPETEMP}
  M106 S0
  TO_THROW_POSITION
  WIPE_ENTER
  WIPE_NOZZLE
  WIPE_STOP
  WIPE_EXIT

  # Clean nozzle must also be at/below probing temp for the first Z probe
  RESPOND TYPE=echo MSG="Cooling to {PROBING_TEMP}C for Z homing..."
  MOVE_HEAT_POS
  M104 S{PROBING_TEMP}
  M106 S255
  TEMPERATURE_WAIT SENSOR=extruder MAXIMUM={PROBING_TEMP}
  M106 S0
  G28 Z ; first probing contact happens with a clean, cold nozzle

  # --- LOAD TOOL ---
  {% if local_skip_purge %}
      # Smart Logic: correct tool already loaded, skip heat-up entirely
      RESPOND TYPE=echo MSG="Smart Logic: Tool {TOOL} already loaded, skipping heat to print temp"
      T{TOOL}
      M400
  {% else %}
      # TOOL is guaranteed >= 0 here (validated above)
      RESPOND TYPE=echo MSG="Heating to {EXTRUDERTEMP}C for loading..."
      TO_THROW_POSITION
      M109 S{EXTRUDERTEMP}
      T{TOOL}
      {% if printer["gcode_macro ACE_DISABLE_FEED_ASSIST"] is defined %}
        ACE_DISABLE_FEED_ASSIST T={TOOL}
      {% endif %}
      G1 E-2 F600
      {% if printer["gcode_macro ACE_ENABLE_FEED_ASSIST"] is defined %}
        ACE_ENABLE_FEED_ASSIST T={TOOL}
      {% endif %}
      M400
      RESPOND TYPE=echo MSG="Cooling back down to {WIPETEMP}C for Wipe/Mesh..."
      MOVE_HEAT_POS
      M106 S255 ; FAN ON
      M109 S{WIPETEMP}
      M106 S0   ; FAN OFF
  {% endif %}

  # --- COOL DOWN & WIPE (needed after load/purge; smart-skip path was already wiped pre-homing) ---
  {% if not local_skip_purge %}
    RESPOND TYPE=echo MSG="Cooling down for Mesh & Wipe ({WIPETEMP}C)..."
    M106 S255        ; fan on to cool down
    M109 S{WIPETEMP} ; Wait for nozzle temp

    TO_THROW_POSITION
    WIPE_ENTER
    WIPE_NOZZLE
    WIPE_STOP
    WIPE_EXIT
  {% endif %}

    RESPOND TYPE=echo MSG="Cooling down for Mesh to {PROBING_TEMP}C..."
    MOVE_HEAT_POS ; move to silicone socket to remove leaking nozzle while cooling down
    M104 S{PROBING_TEMP}
    TEMPERATURE_WAIT SENSOR=extruder MAXIMUM={PROBING_TEMP} ; probing requires nozzle at/below probing temp

    # --- MESH ---
    RESPOND TYPE=echo MSG="Checking Bed Temp..."
    {% if printer.heater_bed.temperature < BEDTEMP %}
        RESPOND TYPE=echo MSG="Heating Bed to {BEDTEMP}C..."
        M190 S{BEDTEMP}
    {% else %}
        RESPOND TYPE=echo MSG="Bed is already at {printer.heater_bed.temperature}C. Proceeding..."
        M140 S{BEDTEMP}
    {% endif %}

    RESPOND TYPE=echo MSG="Z Homing & Mesh..."
    G28 Z

    # --- BED MESH LOGIC (Temperature Based) ---
  {% set bed_int = BEDTEMP|int %}
  {% set MESH_NAME = "BED_" ~ bed_int %}

  {% if printer.bed_mesh.profiles[MESH_NAME] is defined %}
      RESPOND TYPE=echo MSG="Bed mesh '{MESH_NAME}' found. Loading profile..."
      BED_MESH_PROFILE LOAD={MESH_NAME}
  {% else %}
      RESPOND TYPE=echo MSG="Bed mesh '{MESH_NAME}' not found. Generating adaptive mesh..."
      BED_MESH_CALIBRATE PROFILE=adaptive ADAPTIVE=1
  {% endif %}

    # --- PRINT START ---
    RESPOND TYPE=echo MSG="Final Heating..."
    M106 S0              ; fan off
    MOVE_HEAT_POS
    M109 S{EXTRUDERTEMP} ; Heat to printing temp

    TO_THROW_POSITION

    # --- PURGE LOGIK ---
    M83

    # DECISION: skip EVERYTHING?
    {% if not local_skip_purge %}

        # new tool or no tool loaded
        RESPOND TYPE=echo MSG="Purge for new tool..."
        G1 E50 F400
        G1 E60 F150
        G4 P3000
        TO_THROW_POSITION
        TO_FRONT_OF_THROW_BLADE
        G4 P2000
        TO_THROW_POSITION
        G1 E90 F150
        G4 P3000
        G92 E0
        G1 E-2 F1800
        G1 X105 Y320 F18000

    {% else %}
        # --- SMART LOGIC ACTIV ---
        # Same tool + sensor triggered  -> NO PURGE, NO LOAD.
        RESPOND TYPE=echo MSG="Smart Logic active: Skipping Heavy Purge & Reload."
        G1 E2 F300 ; prime

    {% endif %}

    # --- Clear startup_toolchange flag ---
    {% if printer["gcode_macro _ACE_STATE"] is defined %}
      SET_GCODE_VARIABLE MACRO=_ACE_STATE VARIABLE=startup_toolchange VALUE=0
    {% endif %}

    M400

    #Purge Line
    UNDERLINE

    #Make sure defined pause state behaviour in case anything strange happened beforehand
    CLEAR_PAUSE
    SET_IDLE_TIMEOUT TIMEOUT=3600

    RESPOND TYPE=echo MSG="G9111 complete."

[gcode_macro FLUSH_POOP]
description: Flush
gcode:
  TO_THROW_POSITION
  TO_FRONT_OF_THROW_BLADE_SLOW
  G4 P500
  TO_THROW_POSITION
  M106 S255
  G4 P1000
  SHAKE_NOZZLE_HORIZONTAL
  TO_THROW_POSITION
  TO_THROW_POSITION
  M106 S0

[gcode_macro SHAKE_NOZZLE_HORIZONTAL]
description: "Shake nozzle left/right to remove purged filament"
# Optional parameters:
#   AMP: amplitude of shake in mm (default 40)
#   COUNT: number of shake cycles (default 2)
#   SPEED: feedrate for shaking in mm/min (default 20000)
gcode:
    {% set AMP = params.AMP|default(40)|float %}
    {% set COUNT = params.COUNT|default(2)|int %}
    {% set SPEED = params.SPEED|default(20000)|float %}
    {% set AMP_NEG = 0 - AMP %}

    SAVE_GCODE_STATE NAME=shake_nozzle_state

    G91

    {% for i in range(COUNT) %}
        G1 X{AMP} F{SPEED}
        G1 X{AMP_NEG} F{SPEED}
    {% endfor %}

    G90

    RESTORE_GCODE_STATE NAME=shake_nozzle_state
